<Page
    x:Class="FiveInRow.UI.Metro.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FiveInRow.UI.Metro"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:comp="using:FiveInRow.UI.Metro.Components"
    xmlns:conv="using:FiveInRow.UI.Metro.Converters"
    xmlns:f="using:FiveInRow.Foundation"
    xmlns:sys="using:System"
    mc:Ignorable="d">

    <Page.Resources>
        <conv:CellValue2VisibilityConverter x:Key="CP2VisibilityConverter" />
    </Page.Resources>
    
    <Page.BottomAppBar>
        <AppBar>
            <StackPanel Orientation="Horizontal">
                <Button Content="Restart" />
                <Button Content="Undo" />
            </StackPanel>
        </AppBar>
    </Page.BottomAppBar>
    
    <Grid Background="{StaticResource ApplicationPageBackgroundThemeBrush}">
        <ItemsControl ItemsSource="{Binding Board.Cells}" Background="Green">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <comp:BoardPanel />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Grid Width="60" Height="60">
                        <Border Name="el" BorderBrush="Gray" BorderThickness="0" Margin="1" />
                        <Rectangle Stroke="LightGray" StrokeThickness="1" StrokeDashArray="2 2" Fill="White" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" />
                        <Viewbox StretchDirection="DownOnly" Margin="5" x:Name="pX" Visibility="{Binding Value, Converter={StaticResource CP2VisibilityConverter}, ConverterParameter=X}">
                            <Grid>
                                <Line Stroke="Red" StrokeThickness="3" X1="0" Y1="0" X2="60" Y2="60" />
                                <Line Stroke="Red" StrokeThickness="3" X1="60" Y1="0" X2="0" Y2="60" />
                            </Grid>
                        </Viewbox>
                        <Viewbox StretchDirection="DownOnly" Margin="5" x:Name="pO" Visibility="{Binding Value, Converter={StaticResource CP2VisibilityConverter}, ConverterParameter=O}">
                            <Ellipse Stroke="Blue" StrokeThickness="3" Width="60" Height="60" />
                        </Viewbox>
                    </Grid>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</Page>
